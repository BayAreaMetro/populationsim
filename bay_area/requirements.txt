# PopulationSim TM2 EXACT WORKING REQUIREMENTS
# DO NOT USE - Use environment_export.yml instead
# This file is kept for reference only

# CRITICAL: Use Python 3.8.20, NOT 3.12
# Create environment with: conda env create -f environment_export.yml

# Core data processing (EXACT versions from working environment)
pandas==2.0.3
numpy==1.21.0
pyarrow==17.0.0
tables==3.8.0
numexpr==2.8.4
numba==0.58.0

# ActivitySim ecosystem
activitysim==1.1.0
OpenMatrix==0.3.5.0
orca==1.8

# Optimization
ortools==9.12.4544

# Geospatial
geopandas==0.13.2
fiona==1.8.22
shapely==2.0.7
pyproj==3.5.0

# System utilities
psutil==6.0.0
PyYAML==6.0.2
requests==2.32.3

# Census API
census==0.8.24

# Development (optional)
jupyter>=1.0.0
ipykernel>=6.0.0

# NOTE: PopulationSim should be installed separately with:
# pip install -e . (from the populationsim repository root)
